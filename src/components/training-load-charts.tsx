"use client";

import { Bar, CartesianGrid, Cell, ComposedChart, Legend, Line, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import type { TrainingLoadTrend } from "@/lib/training-load";

const colors = { light: "#80b99c", moderate: "#e8a53a", high: "#df5a67", unavailable: "#cbd5cf" } as const;
function dateLabel(value: string): string { return new Intl.DateTimeFormat("de-DE", { day: "2-digit", month: "2-digit" }).format(new Date(`${value}T12:00:00Z`)); }

export function TrainingLoadCharts({ trend }: { trend: TrainingLoadTrend[] }) {
  return <article className="card mt-6 p-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><h2 className="font-bold">Trainingsbelastung</h2><p className="mt-1 text-xs text-[var(--muted)]">Tageslast und rollierende 7-Tage-Summe in UltraPilot Load Points</p></div><div className="flex gap-3 text-xs text-[var(--muted)]"><span><i className="mr-1 inline-block size-2 rounded-full bg-[#80b99c]"/>leicht</span><span><i className="mr-1 inline-block size-2 rounded-full bg-[#e8a53a]"/>mittel</span><span><i className="mr-1 inline-block size-2 rounded-full bg-[#df5a67]"/>hoch</span></div></div><div className="mt-4 h-72"><ResponsiveContainer width="100%" height="100%"><ComposedChart data={trend} margin={{ top: 8, right: 12, bottom: 0, left: 0 }}><CartesianGrid stroke="#e5ebe7" strokeDasharray="4 4" vertical={false}/><XAxis dataKey="date" tickFormatter={dateLabel} minTickGap={24} tick={{ fill: "#66736b", fontSize: 11 }} axisLine={false} tickLine={false}/><YAxis yAxisId="daily" tick={{ fill: "#66736b", fontSize: 11 }} axisLine={false} tickLine={false}/><YAxis yAxisId="rolling" orientation="right" tick={{ fill: "#66736b", fontSize: 11 }} axisLine={false} tickLine={false}/><Tooltip labelFormatter={(label) => dateLabel(String(label))} formatter={(value, name) => [`${Number(value).toFixed(1)} UPL`, String(name)]} contentStyle={{ border: "1px solid #dfe6e1", borderRadius: 12 }}/><Legend/><Bar yAxisId="daily" dataKey="loadPoints" name="Tageslast" radius={[5, 5, 0, 0]}>{trend.map((item) => <Cell key={item.date} fill={colors[item.level]}/>)}</Bar><Line yAxisId="rolling" type="monotone" dataKey="rollingSevenDayLoad" name="7-Tage-Summe" stroke="#173f2c" strokeWidth={3} dot={false} isAnimationActive={false}/></ComposedChart></ResponsiveContainer></div></article>;
}
